import { Routes } from '@angular/router';
import { PokemonSearch } from './pages/pokemon-search/pokemon-search';

export const routes: Routes = [
    { path: 'search', component: PokemonSearch }
];
